#groups.row
  .large-centered-column
    {{#if invites}}

    %ul
      // Invitations
      {{> groups-invites.hbs}}

      // Create group
      {{> groups-create.hbs}}

      // Group Loop
      {{#each groups}} {{> groups-group.hbs}} {{/each}}

    {{else}}

    {{#if groups}}

    %ul
      // Create group
      {{> groups-create.hbs}}

      // Group Loop
      {{#each groups}} {{> groups-group.hbs}} {{/each}}

    {{else}}

    // First group creation
    {{> groups-first.hbs}}

    {{/if}}

    {{/if}}