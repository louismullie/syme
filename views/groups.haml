{{#if groups}}

#groups.row
  .large-centered-column

    {{#if invites}}

    %ul
      // Invitations
      {{> groups-invites.hbs}}

      // Create group
      {{> groups-create.hbs}}

      // Group Loop
      {{#each groups}} {{> groups-group.hbs}} {{/each}}

    {{else}}

    %ul
      // Create group
      {{> groups-create.hbs}}

      // Group Loop
      {{#each groups}} {{> groups-group.hbs}} {{/each}}

    {{/if}}

{{else}}

-# If there are no groups, first group creation
{{> groups-first.hbs}}

{{/if}}