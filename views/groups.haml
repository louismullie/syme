#groups.row
  .small-12.large-8.large-centered.columns
    {{#if invites}}

    %ul
      // Invitations
      {{> groups-invites}}

      // Create group
      {{> groups-create}}

      // Group Loop
      {{#each groups}} {{> groups-group}} {{/each}}

    {{else}}

    {{#if groups}}

    %ul
      // Create group
      {{> groups-create}}

      // Group Loop
      {{#each groups}} {{> groups-group}} {{/each}}

    {{else}}

    // First group creation
    {{> groups-create_first}}

    {{/if}}

    {{/if}}