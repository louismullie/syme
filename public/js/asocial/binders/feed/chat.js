asocial.binders.add('feed', { chat: function(){

  // Shortcut for image attachment links
  $('.userlist-avatar').on('click', function(){
    
    var user_id = $(this).data('user-id');
    
    // Add chatbox

    
  });

} }); // asocial.binders.add();